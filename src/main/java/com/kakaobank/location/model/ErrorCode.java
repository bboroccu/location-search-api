package com.kakaobank.location.model;

public enum ErrorCode {
    SUCCESS,
    NOT_FOUND_ID,
    FAIL_PASSWORD,
    NOT_FOUND_ROLE,
    AUTHENTICATION,
    TOKEN_EXPIRED,
    NOTFOUND_REFRESH_TOKEN,
    UNKNOWN_ERROR,
    ALREADY_USER_EXIST,
    INVALID_PARAMETER;
}
